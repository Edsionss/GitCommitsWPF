<Window x:Class="GitCommitsWPF.Views.AuthorSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择作者" Width="500" Height="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TabControl Grid.Row="0" Margin="10" x:Name="AuthorTabControl">
            <TabItem Header="最近使用" IsSelected="True" x:Name="RecentTab">
                <ListBox Margin="5" x:Name="RecentListBox" />
            </TabItem>
            
            <TabItem Header="扫描记录" x:Name="ScannedTab">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" Margin="5" DockPanel.Dock="Top">
                        <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,5,0" />
                        <TextBox Width="300" Margin="5,0,0,0" x:Name="SearchBox" />
                    </StackPanel>
                    <ListBox Margin="5" x:Name="ScannedListBox" DockPanel.Dock="Top" />
                </DockPanel>
            </TabItem>
        </TabControl>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button Content="选择" Padding="15,5,15,5" Margin="5" MinWidth="80" x:Name="SelectButton" Click="SelectButton_Click" />
            <Button Content="清除记录" Padding="15,5,15,5" Margin="5" MinWidth="80" x:Name="ClearButton" Click="ClearButton_Click" />
            <Button Content="取消" Padding="15,5,15,5" Margin="5" MinWidth="80" x:Name="CancelButton" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window> 